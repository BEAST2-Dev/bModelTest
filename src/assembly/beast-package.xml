<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0 http://maven.apache.org/xsd/assembly-2.2.0.xsd">
    <id>beast-package</id>
    <formats>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>

    <!-- version.xml from project root -->
    <files>
        <file>
            <source>${project.basedir}/version.xml</source>
            <outputDirectory>/</outputDirectory>
        </file>
        <!-- Source JAR at top level (BEAST convention: PKG_NAME.src.jar) -->
        <file>
            <source>${project.build.directory}/${project.build.finalName}-sources.jar</source>
            <destName>${beast.pkg.name}.src.jar</destName>
            <outputDirectory>/</outputDirectory>
        </file>
    </files>

    <!-- Module JAR only (BEAST core, JavaFX, and their transitive deps are provided by the runtime) -->
    <dependencySets>
        <dependencySet>
            <outputDirectory>/lib</outputDirectory>
            <useProjectArtifact>true</useProjectArtifact>
            <scope>runtime</scope>
            <includes>
                <include>io.github.beast2-dev:bModelTest</include>
            </includes>
        </dependencySet>
    </dependencySets>

    <fileSets>
        <!-- fxtemplates/ -->
        <fileSet>
            <directory>${project.basedir}/src/main/resources/bmodeltest/fxtemplates</directory>
            <outputDirectory>/fxtemplates</outputDirectory>
        </fileSet>
        <!-- examples/ -->
        <fileSet>
            <directory>${project.basedir}/examples</directory>
            <outputDirectory>/examples</outputDirectory>
        </fileSet>
        <!-- doc/ -->
        <fileSet>
            <directory>${project.basedir}/doc</directory>
            <outputDirectory>/doc</outputDirectory>
        </fileSet>
        <!-- js/ (analyser web tooling) -->
        <fileSet>
            <directory>${project.basedir}/js</directory>
            <outputDirectory>/js</outputDirectory>
        </fileSet>
    </fileSets>
</assembly>
